import React from 'react';

import { Alert } from 'react-bootstrap';

const SearchFilter = () => {
  return (
    <Alert variant='secondary' className='mt-1 rounded-0 text-center'>
      Search filter is not implemented yet
    </Alert>
  );
};

export default SearchFilter;
